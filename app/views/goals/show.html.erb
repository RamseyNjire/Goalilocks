<h1><%= @goal.title %></h1>

<p>Privacy Status: <%= @goal.is_private == true ? "Private" : "Public" %></p>
<p>Completion Status: <%= @goal.is_complete == true ? "Completed" : "In Progress" %></p>

<pre><%= @goal.description %></pre>

<a href="<%= edit_goal_url(@goal) %>">Update Goal</a>
